---
title: Entity-Type Catalog
phase: 4
status: completed
---

# Phase 4 — Entity-Type Catalog

`i.I()` dispatches on `ax` (entityType). Catalog every type value: role,
constructor source, key fields, update handler, and level usage (from Phase 2
histogram).

## Deliverable

`docs/gameplay-mining/entity-type-catalog.md`: table ax → role → evidence →
confidence; extends `i-av-reconstruction.md`'s type-10 state table and the
known types 0/4/7/10/11/25/69/105.

## Method

- `i.I()` switch arms + `k.d(boolean)` loader + record `Z` parameter reads.
